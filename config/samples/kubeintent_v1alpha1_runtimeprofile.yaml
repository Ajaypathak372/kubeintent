apiVersion: kubeintent.io/v1alpha1
kind: RuntimeProfile
metadata:
  name: production-default
spec:
  defaults:
    availability: gold
    securityTier: hardened
    autoscaling:
      enabled: true
      minReplicas: 3
      maxReplicas: 20
      cpuUtilizationTargetPct: 65
